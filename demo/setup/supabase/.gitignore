# Supabase
.branches
.temp

# dotenvx
.env.keys
.env.local
.env.*.local

# JWT signing keys (contains private keys - never commit!)
signing_key.json

# Deno lockfiles (regenerated by Supabase edge runtime)
**/functions/**/deno.lock
